# Post Templates

`templates.js` defines the available Facebook and Instagram layouts, including the output size, fallback background, and title placement.

Add each approved real-photo background to this folder and update that template's `background` path in `templates.js`. Uploaded photos are temporary browser URLs and are never saved to this folder.
